import styled from "styled-components";
import { colors } from "@theme";
const BigLetter = styled.div`
    position: absolute;
    z-index: 2;
    top: -23px;
    left: 13px;
    font-size: 200px;
    color: ${colors.bigLetter};
    font-weight: 700;
`;
export default BigLetter;
